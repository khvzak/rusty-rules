rules:
  rule1:
    ip: ["127.0.0.1/0"]
    port: {">": 80}
    header(host): ^www\.example\.com$
    any:
      - method: GET
      - method: POST
    not:
      method: PUT
    all:
      param(a): 1
      param(b): 2
